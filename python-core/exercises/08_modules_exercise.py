# Exercise: Modules

# 1. Import the standard "math" module and use it to compute a square root.
# 2. Import the standard "random" module and generate a random number 1-10.
# 3. Import "datetime" and print today's date.

import math
import random
from datetime import date

print("Square root of 81:", math.sqrt(81))
print("Random number 1-10:", random.randint(1, 10))
print("Today's date:", date.today())
